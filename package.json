{
  "name": "library-of-the-citadel",
  "version": "0.1.0",
  "private": true,
  "description": "Library of the Citadel — static Library framework: a hall, a World Recipes gallery, and one generated page per knowledge record. Distinct from Atom-KB's separate Library. No dependencies, no backend.",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "node src/build.mjs",
    "build:preview": "LIBRARY_BASE_PATH=/library-preview/ LIBRARY_OUT_DIR=dist-preview node src/build.mjs",
    "check": "node --test tests/*.mjs",
    "test": "node --test tests/*.mjs"
  },
  "dependencies": {},
  "devDependencies": {}
}
